.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
.git
.idea
chats
logs
tasks
# Secrets must never enter image layers. config.json carries otlp_bearer_token
# and a local .env may carry API keys — keep them out and inject at runtime
# (mount config.json; pass env vars / --env-file).
config.json
.env
.env.*
*.env
